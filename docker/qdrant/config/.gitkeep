# Qdrant Configuration

This directory contains Qdrant collection configuration.

## Collections

- `chapters` - Chapter embeddings with metadata payloads
- `concepts` - Concept embeddings for semantic search

## Vector Configuration

```yaml
vectors:
  size: 384  # sentence-transformers/all-MiniLM-L6-v2
  distance: Cosine
```
